// import { interface } from "os";

export interface RobotType {
    name: string;
    price: string;
    image: string;
    material: string;
    createdAt: string;
    stock: number;
    id: number;

  }

// export default RobotType;
    
